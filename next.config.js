/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel deployment optimized settings
  experimental: {
    // Enable Edge Runtime support for future streaming features
  },
};

export default nextConfig;
